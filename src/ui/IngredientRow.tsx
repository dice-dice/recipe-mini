import styled from "styled-components";

const IndredientRow = styled.div`
  display: grid;
  grid-template-columns: 2fr 1fr 1fr auto;
  gap: 0.8rem;
  align-items: center;
`;

export default IndredientRow;
